server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "dts_meta"
  sslmode: "disable"

log:
  level: "info"
  format: "json"
  output: "stdout"

